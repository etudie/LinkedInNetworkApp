@model WebApplication1.ViewModels.RandomClientViewModel
@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Record.name</h2>
<ul>
    @foreach (var client in Model.Client)
    {
        <li>@client.clientName</li>
    }
</ul>

